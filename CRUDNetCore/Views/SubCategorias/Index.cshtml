@model IEnumerable<CRUDNetCore.Models.SubCategoria>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VchCodigo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VchDescripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UnqGencategoriaLinkNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VchCodigo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VchDescripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UnqGencategoriaLinkNavigation.VchCodigo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UnqGensubCategoriaKey">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UnqGensubCategoriaKey">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UnqGensubCategoriaKey">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
